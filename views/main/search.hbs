<div class="bg search-bg">
  <form action="/main" method="POST" class="form-signin">
    <h2 class="cover-heading urban">Don't know where to go?</h2>
    <p class="lead">Planning a trip was never that easy, book unique places you have dreamed about.</p>
    <label for="budget">How much do you want to spend?</label>
    <input type="text" name="budget" value="" placeholder="R$ 10.000,00" class="form-control">

    <div class="d-flex">
      <label for="dataIda">When to leave?
      <input type="date" name="dataIda" class="form-control"></label>
      <label for="dataVolta">And be back?
      <input type="date" name="dataVolta" class="form-control"></label>
    </div>
    <label for="interesse">What do you like?</label>
    <select name="interesse" id="interesse" class="form-control">
      {{#each result}}
      <option value="{{tipo}}">{{tipo}}</option>
      {{/each}}
    </select>
    <button type="submit" class="btn btn-lg btn-primary logo">Partiu!</button>
  </form>
</div>

